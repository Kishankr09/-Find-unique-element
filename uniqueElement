int main()
{
	int t,i,n,k,c;
	scanf("%d",&t);
	while(t>0)
	{
	    scanf("%d %d",&n,&k);
	    int arr[n];
	    for(i=0; i<n; i++)
	        scanf("%d",&arr[i]);
	    for(i=0; i<n; i++)
	    {
	        c=0;
	        for(int j=0; j<n; j++)
	            {
	                if(arr[i]==arr[j])
	                    c++;
	            }
	           if(c==1)
	                printf("%d \n",arr[i]);
	    }
	    t--;
	}
	return 0;
}
